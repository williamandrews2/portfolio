import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#49bad2",
      contrast: "#e72e5a",
      background1: "#121212",
    },
  },
});

export default theme;
